# Runtime dependencies, pinned to the versions this pipeline was built and
# validated against (Python 3.14).
#
# Note on TensorFlow: the deep-learning model is implemented in PyTorch because
# TensorFlow publishes no cp314 wheels at time of writing. See README.

# --- core data / modelling -------------------------------------------------
numpy==2.4.1
pandas==2.3.3
scipy==1.17.0
scikit-learn==1.8.0
joblib==1.5.3
tabulate==0.10.0           # markdown tables in the generated model report

# --- deep learning ---------------------------------------------------------
torch==2.12.0

# --- warehouse -------------------------------------------------------------
duckdb==1.5.4              # local backend, stands in for BigQuery
pyarrow==25.0.0            # Parquet interchange between stages
xlrd==2.0.2                # UCI ships the source data as legacy .xls

# --- Google Cloud ----------------------------------------------------------
google-cloud-bigquery==3.42.2
google-cloud-storage==3.13.0
google-cloud-logging==3.16.1
db-dtypes==1.7.1           # BigQuery DATE/TIME -> pandas dtypes

# --- serving ---------------------------------------------------------------
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
PyYAML==6.0.3
